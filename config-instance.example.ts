const instanceConfig = {
    hostip: {
        endpoint: "wss://service.tunnelmole.com:8081"
    },
    runtime: {
        debug: true,
        enableLogging: true
    }
}

export default instanceConfig;